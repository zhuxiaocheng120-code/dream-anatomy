<view class="page">
  <view class="archive-panel quick-workbench">
    <view class="panel-content">
      <view class="eyebrow">AI 整理梦境</view>
      <view class="title">写下梦境文字</view>
      <view class="subtitle">写下梦境文字，系统会帮你整理摘要、情绪词、意象关键词和自我反思问题。</view>
      <view class="microcopy">结果仅供记录和回顾，不代表固定说法。</view>
    </view>
    <view class="corner-lines" aria-hidden="true" data-visual="quick-workbench"></view>
  </view>

  <textarea class="input section manuscript-input" value="{{dreamText}}" maxlength="5000" placeholder="例如：我梦见自己在很长的走廊里找一间教室……" bindinput="onDreamInput"></textarea>

  <checkbox-group class="section consent" bindchange="toggleAgreement">
    <label class="small muted">
      <checkbox value="accepted" checked="{{agreed}}" />
      我已阅读并同意
      <text class="link" bindtap="openTerms">《用户协议》</text>
      <text class="link" bindtap="openPrivacy">《隐私政策》</text>
      和
      <text class="link" bindtap="openAi">《AI 使用说明》</text>
    </label>
  </checkbox-group>
  <checkbox-group class="section consent" bindchange="toggleCrossBorderAgreement">
    <label class="small muted">
      <checkbox value="cross-border-accepted" checked="{{crossBorderAgreed}}" />
      我已阅读
      <text class="link" bindtap="openCrossBorder">《境外处理说明》</text>
      ，并单独同意必要的境外处理
    </label>
  </checkbox-group>

  <error-state message="{{errorMessage}}"></error-state>
  <loading-state wx:if="{{submitting}}" text="正在整理梦境记录……"></loading-state>
  <button class="button section" disabled="{{submitting}}" bindtap="submit">保存并整理</button>
</view>
